# Uncomment the next line to define a global platform for your project
 platform :ios, '12.0'

def available_pods
    
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for test_test

   pod 'Kingfisher', '~> 7.0'

end


target 'test_test' do
    
    available_pods
    
end

target 'test_testTests' do
    
    available_pods
    
end
